import MenuBar from "./MenuBar"

export default function MainContainer() {
    return (
        <div className="container">
            <MenuBar />
            <main>
            </main>
        </div>
    )
}